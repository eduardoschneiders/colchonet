<%= render 'search' %>
<h1>
  <% if @search_query.present? %>
    <%= t '.search_results' %>
  <% else %>
    <%= t '.title' %>
  <% end %>
</h1>

<%= render @rooms %>

<%= paginate @rooms %>

<%= link_to (t '.create'), new_room_path %>
